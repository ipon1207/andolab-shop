name: 🐛 バグ報告 (Bug Report)
description: 動作がおかしい、エラーが出るなどの不具合報告はこちら
title: "[Bug]: "
labels: ["bug"]
body:
  - type: markdown
    attributes:
      value: |
        バグ報告ありがとうございます！
        早期解決のために、以下の情報の提供にご協力ください。

  - type: textarea
    id: description
    attributes:
      label: バグの内容
      description: どのような不具合が発生していますか？
      placeholder: 例: 商品購入ボタンを押しても反応がない。
    validations:
      required: true

  - type: textarea
    id: steps
    attributes:
      label: 再現手順
      description: どうすればそのバグが発生しますか？
      placeholder: |
        1. 商品詳細ページを開く
        2. 数量を「3」にする
        3. 「カートに入れる」ボタンをクリックする
        4. エラーが表示される
    validations:
      required: true

  - type: textarea
    id: expected
    attributes:
      label: 期待する動作
      description: 本来はどう動くべきでしたか？
      placeholder: 例: 「カートに追加しました」というメッセージが表示されるべき。
    validations:
      required: true

  - type: dropdown
    id: area
    attributes:
      label: 発生箇所
      options:
        - フロントエンド (画面・表示)
        - バックエンド (API・通信)
        - データベース
        - その他/不明
    validations:
      required: true

  - type: textarea
    id: environment
    attributes:
      label: 環境情報
      description: OS、ブラウザ、端末などを教えてください。
      render: bash
      value: |
        - OS: Raspberry Pi OS / Windows / Mac
        - Browser: Chrome / Safari
    validations:
      required: false